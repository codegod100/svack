{
  "name": "svack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview",
    "svelte-check": "svelte-check --tsconfig ./tsconfig.json",
    "backend:dev": "wrangler dev --config backend/wrangler.toml",
    "backend:deploy": "wrangler deploy --config backend/wrangler.toml"
  },
  "dependencies": {
    "capnweb": "^0.1.0",
    "svelte": "^5.39.11"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.5.14",
    "@rsbuild/plugin-svelte": "^1.0.11",
    "@cloudflare/workers-types": "^4.20250224.0",
    "svelte-check": "^4.3.3",
    "typescript": "^5.9.3",
    "wrangler": "^3.99.0"
  }
}
